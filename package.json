{
  "name": "json-type",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "prepare": "yarn build",
    "build": "rm -rf dist/ && tsc",
    "test": "tsc ./test/index.ts --noEmit"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "prettier": "^2.2.0",
    "typescript": "^4.1.2"
  }
}
